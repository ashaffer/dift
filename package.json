{
  "name": "dift",
  "description": "Super fast list diff algorithm",
  "repository": "git://github.com/ashaffer/dift.git",
  "version": "0.1.5",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "prepublish": "rm -rf lib && babel src --out-dir lib",
    "postpublish": "rm -rf lib"
  },
  "dependencies": {
    "bit-vector": "^0.1.0"
  },
  "devDependencies": {
    "babel-tape-runner": "^1.3.0",
    "babelify": "^7.2.0",
    "standard": "^5.1.0",
    "tape": "^4.2.0"
  }
}
